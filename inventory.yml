all:
  children:
    prometheus:
      hosts:
        promserver:
          ansible_host: 172.16.16.129 
          ansible_user: test 
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    target:
      hosts:
        prom-target1:
          ansible_host: 127.0.0.1
